document.addEventListener("DOMContentLoaded", () => {

    // Placeholder citizen status
    const statusBox = document.getElementById("citizen-status-box");
    statusBox.innerText = "Visitor (Trial Not Started)";

    // Placeholder trial timer
    const timerBox = document.getElementById("trial-timer-box");
    timerBox.innerText = "Trial timer will activate upon onboarding.";

});
